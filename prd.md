# dsh-qr-launcher 产品需求文档 (PRD)

---

## 1. 文档信息

| 项目 | 内容 |
| :--- | :--- |
| **产品名称** | dsh-qr-launcher |
| **版本** | v1.0.0 |
| **状态** | 草稿 / 开发中 |
| **目标用户** | DeepSeek Harness (DSH) 使用者，尤其需要在局域网内通过手机快速访问 DSH 的用户 |
| **设计原则** | 零侵入、轻依赖、即装即用 |

---

## 2. 项目背景

### 2.1 现状与痛点
- DSH 默认仅监听 `127.0.0.1`，手机无法直接访问。
- 即使通过插件（如 `dsh-lan-access`）开启局域网访问，用户仍需手动复制带 Token 的长链接，在手机浏览器中粘贴，操作繁琐。
- 现有社区插件要么功能过于庞大（附带多余功能），要么不提供直观的二维码展示。

### 2.2 目标
提供一个**纯二维码生成与展示**的轻量级插件，在终端和 Web 界面中自动显示可扫码访问的局域网链接，实现“手机扫一扫，即刻用 DSH”。

---

## 3. 产品范围

### 3.1 包含范围
- 在 DSH 启动时，终端自动输出访问二维码（ASCII 字符画）。
- 在 DSH Web 界面的“设置”页面中嵌入二维码面板。
- 自动检测本机局域网 IP 并拼接 Token 生成完整 URL。
- 支持 DSH 标准的 Token 认证机制。

### 3.2 不包含范围
- 不涉及网络代理、端口转发、公网穿透等。
- 不修改 DSH 核心网络绑定逻辑（由 `dsh-lan-access` 等负责）。
- 不提供用户认证或权限管理（复用 DSH 原生 Token）。
- 不支持多网卡选择（固定取第一个非内部 IPv4 地址）。

---

## 4. 功能需求

### 4.1 终端二维码显示

| 需求编号 | 功能点 | 优先级 | 说明 |
| :------- | :----- | :----- | :--- |
| FR-01 | 在 DSH `ready` 事件触发后，自动生成二维码 | P0 | 确保 DSH 已完全启动，端口已监听 |
| FR-02 | 二维码内容为完整的局域网访问 URL（含 Token） | P0 | 格式：`http://{ip}:3080/?token={token}` |
| FR-03 | 使用 ASCII 字符画输出二维码 | P0 | 依赖 `qrcode-terminal` 或类似库 |
| FR-04 | 同时输出明文 URL 作为备选 | P1 | 方便复制或文本传输 |
| FR-05 | 支持通过环境变量或命令行参数禁用终端二维码 | P2 | 例如 `--no-qr-terminal` |

### 4.2 Web 界面二维码显示

| 需求编号 | 功能点 | 优先级 | 说明 |
| :------- | :----- | :----- | :--- |
| FR-06 | 在 DSH Web 设置页新增“二维码访问”面板 | P0 | 位置建议在“常规”或“网络”设置区 |
| FR-07 | 面板内显示当前局域网访问二维码（含 Token） | P0 | 使用 SVG/Canvas 渲染二维码（如 `qrcode.react`） |
| FR-08 | 二维码下方显示可复制的明文 URL | P1 | 方便在电脑上复制分享 |
| FR-09 | 二维码自动刷新（如 IP 变化时） | P2 | 监听网络变化或通过手动刷新按钮 |

### 4.3 局域网 IP 获取

| 需求编号 | 功能点 | 优先级 | 说明 |
| :------- | :----- | :----- | :--- |
| FR-10 | 自动获取本机有效局域网 IPv4 地址 | P0 | 排除 `127.0.0.1`，排除虚拟网卡（如 VMware、VPN） |
| FR-11 | 当获取失败时给出明确错误提示 | P1 | 终端打印“未检测到局域网 IP，请检查网络” |
| FR-12 | 支持多网卡环境下手动指定 IP（环境变量或配置文件） | P2 | 如 `DSH_QR_IP=192.168.1.100` |

### 4.4 依赖与兼容性

| 需求编号 | 功能点 | 优先级 | 说明 |
| :------- | :----- | :----- | :--- |
| FR-13 | **零代码侵入**：不修改 DSH 官方任何文件 | P0 | 通过插件机制独立加载 |
| FR-14 | **最小依赖**：仅引入二维码生成库 | P0 | 终端用 `qrcode-terminal`，Web 用 `qrcode.react` 或纯 JS 库 |
| FR-15 | 兼容 DSH v1.x 及后续版本 | P0 | 使用稳定的 Cordis 插件 API |
| FR-16 | 支持 Node.js 18+ | P0 | 与 DSH 要求的 Node 版本一致 |

---

## 5. 非功能需求

| 类别 | 要求 |
| :--- | :--- |
| **性能** | 二维码生成耗时 < 100ms，不影响 DSH 启动速度 |
| **安全性** | 不存储 Token；二维码仅在本地终端或本地 Web 页面显示；建议在可信网络中使用 |
| **可维护性** | 代码结构清晰，遵循 DSH 插件开发规范，提供简明的 README 和安装指南 |
| **可扩展性** | 后续可考虑支持公网访问二维码（如通过 `ngrok` 等），但当前版本不涉及 |
| **用户体验** | 终端二维码字体大小适中，Web 二维码尺寸可调整（默认 200x200） |

---

## 6. 技术方案概要

### 6.1 架构
- 插件作为一个独立 npm 包，导出 `apply` 函数。
- 通过 Cordis 的 `ctx.on('ready')` 监听启动事件。
- 使用 `os.networkInterfaces()` 获取局域网 IP。
- 终端部分：使用 `qrcode-terminal` 库生成字符画。
- Web 部分：利用 DSH 的 `ctx.ui` 或 `ctx.slot` 注入设置页面自定义面板，使用 `qrcode` 或 `qrcode.react` 渲染二维码。

### 6.2 关键依赖
- `qrcode-terminal`：终端 ASCII 二维码生成（仅开发依赖，或运行时依赖）。
- `qrcode.react` 或 `qrcode-generator`：Web 端二维码渲染。
- 无需额外引入大型框架。

### 6.3 与现有插件的兼容性
- 可与 `dsh-lan-access` 共同使用（后者负责开启局域网监听，本插件负责展示二维码）。
- 不能与同类型的二维码插件（如 `dsh-pocket`）同时使用，建议在文档中说明。

---

## 7. 安全与隐私

- **Token 暴露风险**：二维码中包含 Token，任何人扫码即可获得完整访问权限。因此，插件应提供醒目安全提示，建议仅在可信局域网内使用。
- **终端日志安全**：二维码会打印在终端，请用户注意避免屏幕共享或录制时泄露。
- **Web 面板访问**：Web 界面中的二维码仅对已登录 DSH 的管理员可见（DSH 原生权限控制）。
- 未来可考虑增加 Token 过期时间或一次性使用特性，但当前版本不作要求。

---

## 8. 验收标准 (Acceptance Criteria)

| 编号 | 验收项 | 通过条件 |
| :--- | :----- | :------- |
| AC-01 | 插件安装成功 | `dsh plugin --profile web list` 中出现 `dsh-qr-launcher` |
| AC-02 | 终端显示二维码 | 重启 DSH 后，终端输出包含可识别的 ASCII 二维码块 |
| AC-03 | 二维码可扫描 | 使用手机扫描后，能正确跳转到 DSH 登录页（或直接进入界面） |
| AC-04 | Web 设置页显示二维码 | 在 DSH 设置页中找到二维码面板，并渲染出可识别的二维码 |
| AC-05 | IP 自动检测 | 正确显示本机局域网 IP（非 `127.0.0.1`） |
| AC-06 | Token 正确拼接到 URL | 扫描后访问链接携带有效 Token，无需额外认证 |
| AC-07 | 无干扰其他功能 | 安装后 DSH 原有功能（聊天、会话、设置等）一切正常 |
| AC-08 | 卸载干净 | 执行 `dsh plugin --profile web remove dsh-qr-launcher` 后，终端和 Web 均不再显示二维码 |

---

## 9. 发布计划与版本演进

### v1.0.0（MVP）
- 实现终端 + Web 双端二维码展示。
- 仅支持局域网 IP 自动检测。
- 提供基本的安全提示。

### v1.1.0（后续规划）
- 支持手动指定 IP 和端口。
- 支持二维码有效期（如 5 分钟后失效）。
- 提供选项禁用 Web 面板或终端输出。

### v2.0.0（长期规划）
- 支持公网穿透配合（如自动检测 ngrok 生成的公网 URL 并生成二维码）。
- 多网卡智能选择。

---

## 10. 附录

### 10.1 术语表
- **DSH**：DeepSeek Harness，DeepSeek 官方 AI 应用开发框架。
- **Cordis**：DSH 使用的插件系统底层框架。
- **Token**：DSH 启动时生成的一次性访问令牌，用于浏览器会话认证。
- **局域网 IP**：设备在同一子网内的私有 IP 地址（如 `192.168.x.x`）。

### 10.2 参考文档
- [DSH 插件开发官方文档](https://github.com/deepseek-ai/deepseek-harness)
- [Cordis 框架文档](https://cordis.js.org/)
- `qrcode-terminal` npm 包
- `qrcode.react` npm 包

---

**文档结束**